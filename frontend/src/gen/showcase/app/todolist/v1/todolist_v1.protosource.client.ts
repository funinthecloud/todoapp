
// Code generated by protoc-gen-protosource-ts. DO NOT EDIT.

import type { ProtosourceClient, CommandResponse, History } from "@protosource/client";
import { CreateSchema, RenameSchema, ArchiveSchema, UnarchiveSchema, AddItemSchema, UpdateItemSchema, RemoveItemSchema, TodoListSchema, TodoListListSchema } from "./todolist_v1_pb.js";
import type { Create, Rename, Archive, Unarchive, AddItem, UpdateItem, RemoveItem, TodoList, TodoListList, TodoItem } from "./todolist_v1_pb.js";

const routePath = "showcase/app/todolist/v1";

export class TodoListHTTPClient {
  private readonly client: ProtosourceClient;

  constructor(client: ProtosourceClient) {
    this.client = client;
  }

  async create(id: string, name: string): Promise<CommandResponse> {
    return this.client.apply(routePath, CreateSchema, { id, name });
  }

  async rename(id: string, name: string): Promise<CommandResponse> {
    return this.client.apply(routePath, RenameSchema, { id, name });
  }

  async archive(id: string): Promise<CommandResponse> {
    return this.client.apply(routePath, ArchiveSchema, { id });
  }

  async unarchive(id: string): Promise<CommandResponse> {
    return this.client.apply(routePath, UnarchiveSchema, { id });
  }

  async addItem(id: string, item: TodoItem): Promise<CommandResponse> {
    return this.client.apply(routePath, AddItemSchema, { id, item });
  }

  async updateItem(id: string, item: TodoItem): Promise<CommandResponse> {
    return this.client.apply(routePath, UpdateItemSchema, { id, item });
  }

  async removeItem(id: string, itemId: string): Promise<CommandResponse> {
    return this.client.apply(routePath, RemoveItemSchema, { id, itemId });
  }

  async load(id: string): Promise<TodoList> {
    return this.client.load(routePath, id, TodoListSchema);
  }

  async get(id: string): Promise<TodoList> {
    return this.client.get(routePath, id, TodoListSchema);
  }

  async history(id: string): Promise<History> {
    return this.client.history(routePath, id);
  }

  async queryByCreateBy(createBy: string): Promise<TodoList[]> {
    const params: Record<string, string> = {
      "create_by": createBy,
    };
    const list = await this.client.query(routePath, "by-create-by", params, TodoListListSchema);
    return list.items;
  }

  async queryByCreateByWithCreateAt(createBy: string, skOp: string, createAt: bigint): Promise<TodoList[]> {
    if (skOp === "between") {
      throw new Error("use queryByCreateByBetweenCreateAt for between queries");
    }
    const params: Record<string, string> = {
      "create_by": createBy,
      "sk_op": skOp,
      "create_at": String(createAt),
    };
    const list = await this.client.query(routePath, "by-create-by", params, TodoListListSchema);
    return list.items;
  }

  async queryByCreateByBetweenCreateAt(createBy: string, createAtFrom: bigint, createAtTo: bigint): Promise<TodoList[]> {
    const params: Record<string, string> = {
      "create_by": createBy,
      "sk_op": "between",
      "create_at": String(createAtFrom),
      "create_at2": String(createAtTo),
    };
    const list = await this.client.query(routePath, "by-create-by", params, TodoListListSchema);
    return list.items;
  }

}
